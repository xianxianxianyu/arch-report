# Agent 服务器架构雷达：自动化备份

保存时间：2026-07-27（Asia/Shanghai）

## 当前配置

- 名称：Agent服务器架构雷达（每4小时）
- 状态：启用
- 周期：周一至周五，每 4 小时
- 模型：`gpt-5.6-sol`
- 推理强度：`high`
- 执行环境：本地项目
- Windows 项目路径：`D:\code\graphwiki\archreport`

> 这个文件是迁移备份，不会自行创建自动化。Ubuntu 上必须重新创建自动化，并把绝对路径改成实际目录。

## 当前自动化中的原始 prompt

```text
你是持续运行的“Agent 服务器架构雷达”。每次运行必须在 D:\code\graphwiki\archreport 先完整阅读 CODEX_TASK.md、README.md、state/item-cache.json、state/seen-items.json、state/run-log.jsonl 和 index.md，然后严格按 CODEX_TASK.md 执行一轮增量发现、核验、评分、去重、报告、验证和原子状态推进。只研究运行时点之前滚动 30 天内首次公开或可证明发生实质性更新的高质量 blog/paper，服务于 Agent 服务器应用的系统/软件架构落地；目标 5 篇，但绝不以重复、旧文、营销稿或弱证据凑数。每篇必须形成“具体故障→架构机制→采用设计→验收不变量→边界风险”，区分来源事实/作者主张与工程推断，满足 E/R 师出有名门槛和总分/分项硬门槛。技术结论只依赖原始论文、正式文档、官方工程博客、官方代码仓库等一手来源。周期由本自动化负责；不得创建或修改其他自动化。结束时用中文报告本轮篇数、最高推荐、覆盖问题、报告绝对路径、剩余合格库存及任何失败或不足原因。
```

## Ubuntu 迁移模板

先把 `<PROJECT_DIR>` 替换为这个资料包在 Ubuntu 上的绝对路径，再用于创建自动化：

```text
你是持续运行的“Agent 服务器架构雷达”。每次运行必须在 <PROJECT_DIR> 先完整阅读 CODEX_TASK.md、README.md、state/item-cache.json、state/seen-items.json、state/run-log.jsonl 和 index.md，然后严格按 CODEX_TASK.md 执行一轮增量发现、核验、评分、去重、报告、验证和原子状态推进。只研究运行时点之前滚动 30 天内首次公开或可证明发生实质性更新的高质量 blog/paper，服务于 Agent 服务器应用的系统/软件架构落地；目标 5 篇，但绝不以重复、旧文、营销稿或弱证据凑数。每篇必须形成“具体故障→架构机制→采用设计→验收不变量→边界风险”，区分来源事实/作者主张与工程推断，满足 E/R 师出有名门槛和总分/分项硬门槛。技术结论只依赖原始论文、正式文档、官方工程博客、官方代码仓库等一手来源。周期由本自动化负责；不得创建或修改其他自动化。结束时用中文报告本轮篇数、最高推荐、覆盖问题、报告绝对路径、剩余合格库存及任何失败或不足原因。
```

## 恢复时必须保持的边界

- 同一份 `state/` 不能由两台机器并发运行，否则候选池、去重状态和发布日志可能相互覆盖。
- 第一次运行前应确认没有遗留的 `queued` 条目，且三个状态文件均可解析。
- 自动化产生新报告后，运行 `python3 tools/build_index.py`，更新离线首页与校验清单。
- 如果希望周末也运行，需要在 Ubuntu 新建自动化时明确选择每天；当前备份配置只覆盖周一至周五。
